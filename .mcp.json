{
  "mcpServers": {
    "mem0-gus": {
      "command": "sh",
      "args": [
        "-c",
        "if [ -f ~/.claude/gus.env ]; then set -a; . ~/.claude/gus.env; set +a; fi; exec python3 .claude/mcp/mem0_server.py"
      ]
    },
    "gus": {
      "command": "sh",
      "args": [
        "-c",
        "if [ -f ~/.claude/gus.env ]; then set -a; . ~/.claude/gus.env; set +a; fi; exec python3 .claude/mcp/gus_server.py"
      ]
    }
  }
}
