{
  "mcpServers": {
    "mem0-gus": {
      "command": "sh",
      "args": [
        "-c",
        "pip install -q --user -r .claude/mcp/requirements.txt >/dev/null 2>&1; exec python3 .claude/mcp/mem0_server.py"
      ],
      "env": {
        "MEM0_API_KEY": "${MEM0_API_KEY}"
      }
    }
  }
}
