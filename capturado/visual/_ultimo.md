---
tipo: ultima-captura-visual
captura: (nenhuma ainda)
status: aguardando-primeira-captura
projeto: futuro
---

# Última captura visual

**PROJETO FUTURO** — integração câmera S8 + Claude Vision.

Nenhuma captura realizada ainda. Quando o projeto estiver ativo:
1. Abrir a PWA em `https://<railway-url>/camera` no S8
2. Inserir o `CUSTOM_GPT_TOKEN` na primeira abertura
3. Ativar modo Auto (30s) ou capturar manualmente
4. Custom GPT passa a ler este arquivo via `ver_ultima_captura`

Ver documentação em `projetos/gus/gus-12-portas-futuras.md`.
