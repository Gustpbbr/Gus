---
tipo: stream
semana: 2026-05-05
---

# Stream da semana — 05/05/2026

## 2026-05-07 — Claude Code

### Feito

- **Merge PR #110** (feat: retro engine agente SDK + briefing agente + hub endpoints internos) — 36 conflitos resolvidos preservando todos os changes críticos de ambas as branches
- **`/gpt/contexto`** — novo endpoint agregado em `api/gpt_inbox.py`: chama inbox gpt-chat + hub stats em paralelo via `asyncio.gather()`. GPT Chat só precisa de GPT_INBOX_SECRET (HUB_READ_TOKEN fica interno ao Railway)
- **`gpt-chat-bootstrap.md` v1.8** — reescrito com token via ChatGPT Memory `[Gus]`, endpoint único `/gpt/contexto`, seção "por que não GitHub search", identidade do Gus. Elimina fricção de token por aba
- **Leitura recursiva no inbox** — `_gh_list_recursive()` adicionada ao `api/gpt_inbox.py`; subpastas de `dialogos/inbox-{porta}/` agora aparecem no retorno do endpoint
- **Skills customizadas** — criadas 3 slash commands em `.claude/commands/`: `/status-gus` (painel de estado), `/gerar-demanda` (cria demanda com frontmatter correto), `/auditar-curador` (audita qualidade dos fragmentos do Hub)
- **Levantamento Custom GPT** — mapeamento completo do impacto de criar a porta Custom GPT (separada da porta GPT Chat): infraestrutura parcial existe (`inbox-custom-gpt/`, `custom-gpt` em `_PORTAS_VALIDAS`, `api/routes.py` já tem Actions API)
- **Sync GitHub ⇄ Drive** — migrado de GitHub Actions + OAuth para Google Apps Script bidirecional (projeto `Gus Sync (GitHub ⇄ Drive)`). Apps Script roda com identidade nativa do Gustavo — sem auth externa, sem expirar
- **Bootstrap `claude/review-project-structure-esL3J`** — branch sincronizada com main

### Não feito

- **Deletar fragmento MGX incorreto** — bloqueado: HUB_READ_TOKEN ausente neste ambiente. UUID: `0329c718-36be-4727-b967-36d99a450781`. Deletar via TioGu ou Railway console
- **Levantamento fragmentos curador=gpt** — bloqueado pelo mesmo motivo. Usar `/auditar-curador gpt` em sessão com Hub acessível
- **Bootstrap v1.9 (Contexto Gustavo/Gus)** — proposto mas Gustavo adiou ("vamos deixar assim por enquanto")
- **Custom GPT setup** — Gustavo estava configurando ao fim da sessão; continuar quando retornar

### Próximos passos

- Custom GPT: criar `dialogos/_bootstrap/custom-gpt-bootstrap.md` + confirmar OpenAPI spec
- Validar Apps Script Drive ⇄ GitHub (demanda agendada 14/05)
- Fragmentos MGX e auditoria curador=gpt quando Hub estiver acessível
