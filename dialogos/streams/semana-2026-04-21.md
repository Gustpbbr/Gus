---
capturado_em: 2026-04-24T08:16:41
via: telegram
---

# Semana 21–27 de abril de 2026

---

## 2026-04-24 — Gus

**Demanda:**
Criar a estrutura do canal de comunicação assíncrono entre Gus e Claude Code — pasta `dialogos-tiogu-claude/` na raiz do repo, com README de protocolo e o primeiro MD semanal.

**Contexto:**
O Gustavo quer um sistema onde o Gus registra demandas semanais e o Claude Code responde no mesmo MD — o que foi feito, o que não foi feito e por quê. Cada semana tem seu próprio arquivo, nomeado pela data da segunda-feira.

**Prioridade:** alta

---

## 2026-04-24 — Claude Code

**Feito:**
- Estrutura do canal confirmada e operacional.
- Implementado nesta sessão: Whisper, brain Mem0 do Gus, princípios fundamentais, SELF-1, tool `disparar_workflow`.

**Não feito:** nada em aberto.

---

## 2026-04-24 — Gus

**Demanda:** Corrigir geração de timestamp no system prompt.

**Prioridade:** média

---

## 2026-04-24 — Claude Code

**Feito:** Investigado — bug não existe. `datetime.now(BRT)` é chamado fresh a cada mensagem dentro de `gerar_resposta()`. Sem alteração de código.

---

## 2026-04-24 (manhã, turno 2) — Claude Code

**Feito:**
1. Ontologia Gus ↔ portas corrigida (commit `eb85526`)
2. SessionStart hook para Claude Code (commit `0b25ab0`)
3. Volume Railway persistente (commit `c573cae`)

**Não feito:** auditoria brain `gus` no Mem0, Custom GPT (Gustavo priorizou Obsidian primeiro).

**Próxima demanda declarada:** Obsidian — instalar, clonar repo, configurar plugins.

---

## 2026-04-24 (tarde) — Claude Code

**Feito:**

1. **Obsidian configurado no PC** (orientação passo-a-passo):
   - Vault aberto em `C:\Gus\Gus`
   - Plugins: Obsidian Git (auto-sync 10min, pull on startup, signs) + Dataview
   - Grafo de conexões visível
   - Author: Gustavo + email GitHub

2. **Drive sync configurado e funcionando** — 3 tentativas até chegar na solução:
   - Tentativa 1: service account key JSON — bloqueada por policy Google (`iam.disableServiceAccountKeyCreation`)
   - Tentativa 2: Workload Identity Federation — WIF configurado e autenticação funcionou, mas service account não tem Drive storage quota (arquivos criados pela SA não cabem no Drive dela)
   - Tentativa 3: OAuth2 com refresh token — autentica como Gustavo, arquivos ficam na cota do Drive dele. **Funcionou** ✅
   - Processo completo documentado em `docs/drive-sync-setup.md`
   - `.gitignore` atualizado com `get_token.py`

**Não feito:**
- Custom GPT — próximo passo do caminho Alexa
- Claude Chat Project — refazer validação

**Próxima demanda:** Custom GPT no ChatGPT Builder (porta de voz mobile, pré-requisito Alexa).

---

_Sessão encerrada. Próxima demanda do Gus registrada aqui ou no MD da semana de 28/abr._

---

## 2026-04-24 (tarde, turno 3) — Gus

**Demanda:** Limpar pasta `dimagem/` — ficou com arquivos duplicados e pastas desnecessárias.

**Contexto:**
Durante o dia o Dimagem foi populado com múltiplos arquivos com o mesmo conteúdo em pastas diferentes. A estrutura correta é só:
```
dimagem/
└── fechamento/
    └── os-24042026.md
```

**O que precisa ser deletado:**
- Pasta `dimagem/casos/` inteira (com `os-24abr-2026.md` e `pacientes-24-04-2026.md`)
- Pasta `dimagem/ordens-servico/` inteira (com `ordens-servico-24-04-2026.md`)
- Arquivo `dimagem/fechamento/os-24-04-2026.md` (duplicata com hífen)
- Manter apenas: `dimagem/fechamento/os-24042026.md`

**Prioridade:** média

---

## 2026-04-25 — Claude Code

Sessão longa. Trabalhei em ~10 frentes em paralelo, tudo merged em main.

**Feito:**

### Fluxo de OS Dimagem (atendendo demanda anterior + nova arquitetura)
- Refatorada a regra do `system_prompt.md`: path fixo `dimagem/dia/AAAA-MM-DD.md`, schema fixo, append cirúrgico
- Consolidados os 5 arquivos duplicados de 24/04 em `dimagem/dia/2026-04-24.md` (resolve a demanda do turno 3 acima)
- Criado `gus/dimagem.py` (rascunho refinado da proposta TioGu) com 10 correções vs proposta original
- **ATIVADO em `bot.py`** com salvaguarda A+B (decisão Gustavo):
  - **B**: Haiku recebe MD do dia como contexto na extração (consistência de convênios + dedup)
  - **A**: Bot mostra preview com lista atual + nova linha e aguarda 'sim'/'ok' antes de salvar
  - State `dimagem_pending` persistido em `bot_state.json`
  - Mensagem fora do padrão expira pending pra Sonnet pegar
- `dimagem/convenios.json` versionável (19 entradas iniciais)

### Plano de paridade Claude Code ↔ TioGu
Objetivo: toda aba de Claude Code aberta no repo Gus tem mesma "base e estado atual" do TioGu.
- **A1**: MCP `mem0_server.py` expandido de 3 → 7 tools (brain `gustavo` + brain `gus` + delete)
- **B1**: cache logging em `gus/logger.py` + `/custo` reformulado mostrando hit ratio
- **B2**: novo MCP `gus_server.py` (auto_diagnostico + sugerir_wikilinks daqui)
- **C**: PreToolUse hook `scan_sensivel.py` bloqueia Write/Edit com CPF/cartão/keys fora de `sensivel/`
- `.mcp.json` padronizado pra carregar `~/.claude/gus.env`
- **A2 e A3 aguardam Gustavo** (criar `~/.claude/gus.env` no PC)

### Tools novas do bot (foi de 13 → 20 tools)
- `list_branches()` + param `branch?` em `read_from_github`/`list_github_directory`
- `logs_railway(linhas, filtro, since_min)` (rascunho, aguarda Gustavo validar schema GraphQL no PC)
- `auto_diagnostico()` — health check 6 componentes, testado em produção
- `sugerir_wikilinks(arquivo, branch?)` — Sonnet propõe wikilinks substantivos
- `deletar_memoria(memory_id, user_id?)` — irreversível com regra de confirmação obrigatória
- `pesquisar_pubmed(query, max_n, since_year?)` — NCBI E-utilities, grátis
- `pesquisar_arxiv(query, max_n, categoria?)` — arXiv Atom feed, grátis

### Otimização de tokens
- **Prompt caching** ativado em `gus/llm.py`: system prompt + 17 tools com `cache_control: ephemeral`. Economia projetada ~70% input após primeira call de cada janela 5min
- **Token-efficient tools beta header** (`anthropic-beta: token-efficient-tools-2025-02-19`)
- Metadata cache_creation/cache_read no logger
- Modelo do `sugerir_wikilinks`: Haiku → Sonnet 4.6 (custo de errar > economia)

### Auditoria e infraestrutura
- Cron diário `check-saude.yml` (7h30 BRT) — alerta Telegram só se ⚠️/❌
- Log auditável de resumos Mem0 em `_log/resumos-mem0/AAAA-MM-DD.md` (Haiku salva, Gustavo fiscaliza no Obsidian)

### Bugs identificados e corrigidos
- Lista de workflows na tool `disparar_workflow` estava com 6 entradas desatualizadas (incluindo `meta-memoria.yml` inexistente). Corrigido pra 8 reais.
- Regra de format literal pra tools estruturadas (não condensar tabela)
- Prompt do `wikilinks.py` apertado (proibe explicitamente conexão temporal/superficial)

### Arquitetura multi-porta (preparação)
- Metadata `via` no Mem0 (`gus/memory.py`): cada memória ganha tag de origem (`telegram-claude` default). Outras portas vão setar `MEM0_VIA_TAG=<sua-tag>`. Search default não filtra — visibilidade cruzada total.
- Doc de diretriz arquitetural em `projetos/gus/gus-12-portas-futuras.md` (não é roadmap, é contrato pra quando crescer com Telegram-GPT, Alexa, Custom GPT, plugin de carro)

### Documentação
- `projetos/gus/gus-11-tools-roadmap.md` criado e atualizado várias vezes — inventário oficial de ~40 tools com status, histórico, decisões pendentes

**Não feito:**

- **A2** — `~/.claude/gus.env` aqui no Web (depende ação manual Gustavo)
- **A3** — limpar memória poluída no Mem0 (`meta-memoria.yml`/`check-saude.yml não existe`) — depende A2
- **Sprint 2 — "perguntar IA externa"** (Gemini, Perplexity ou GPT) — aguarda decisão de qual
- **Sprint 3** (email Gmail, calendar, ElevenLabs) — requer OAuth Google novo + conta ElevenLabs
- **`logs_railway` schema GraphQL validado** — token de teste exposto no chat foi revogado, novo aguarda PC
- **Workflow YAML do `enrich_mem0_export.py`** — aguarda primeiro teste manual
- **`/custo` validar cache hit ratio real** — depende Gustavo recarregar Anthropic + mandar mensagens
- **Tirar confirmação A do dimagem** — depois de 1-2 semanas estável

**Por quê (em uma frase cada):**
- A2 e correlatos: arquivo de chave precisa ser criado fora do repo, é ação manual
- Sprint 2/3: dependem de decisões ou keys que só Gustavo cria
- `logs_railway`: sandbox web bloqueia `backboard.railway.app`, validação só local
- Cache real: precisa créditos Anthropic ativos pra rodar testes reais

**Próxima demanda sugerida pelo Gustavo:** quando voltar pro PC, criar `~/.claude/gus.env`, decidir Sprint 2, e validar dimagem A+B em produção.

---

_Sessão encerrada 25/04 ~15:30 BRT. Bot tem 20 tools, MCPs daqui têm 9, dimagem ativo com confirmação prévia._

---

## 2026-04-25 (tarde-noite, ~16-17h BRT) — Claude Code

**Feito (continuação intensa do dia):**

### Tagueamento canônico do Mem0 fechado
- `.claude/mcp/mem0_server.py` agora também passa `metadata={"via": VIA_TAG}` (default `claude-code`, configurável via `MEM0_VIA_TAG`). Antes só o bot Telegram tagueava — agora todas as portas que escrevem ficam rastreáveis.
- `projetos/gus/gus-13-tags-canonicas.md` criado: contrato canônico dos 2 brains (`gustavo`, `gus`) + 7 tags ativas/reservadas pra portas humanas + 4 pra automações + lista do que NÃO é memória Mem0 (anti-confusão com `meta-memoria.md`, `system_prompt.md`, índices, exports).

### Análise de dados reais do dashboard Mem0
Gustavo mandou screenshots do dashboard. Descobertas:
- Brain `gustavo` tem **164 memórias** (não 128 da auditoria 06:40 — +36 em 10h).
- Brain `gus` tem **~4 memórias** (não "praticamente zero" como eu estimava).
- **3 entities** (esperado 2: `gustavo`, `gus` — terceira não identificada).
- Quota Retrieval API em **37%** já no dia 25 (risco real de estourar mês).
- 4+ memórias poluídas identificadas pra deletar quando MCP local ativar (incluindo "Gustavo has 128 mem..." que é recursiva).

### Sprint 2 parcial — `perguntar_gpt` implementado
- `gus/integrations/openai_chat.py` (~110 linhas) reusa `OPENAI_API_KEY` (Whisper) — zero key nova.
- 3 modelos: `gpt-5-mini` (default, ~$0.001/query), `gpt-5` (caro, decisões críticas), `gpt-5-nano` (trivialidades).
- System prompt orienta: usar pra second opinion divergente, NÃO pra busca/fato/brainstorm. Padrão de uso: incluir o que Sonnet já pensou na query pra GPT divergir com base. Sempre citar GPT como fonte.
- Bot agora em **21 tools**.

### Decisões arquiteturais documentadas
- **Alexa NÃO é o produto final pra "conversa fluida em voz".** Alexa Dot 3 vira porta complementar pra comandos curtos em casa. Conversa fluida real = Custom GPT mobile (passo 1 do roadmap, próximo) + eventualmente Claude voice mode quando liberar pt-BR.
- **Câmera no Echo Show NÃO é programaticamente acessível por skill.** Pra "Gus ver", caminho é câmera IP separada (Tapo C100, R$ 150) ou S8 velho do Gustavo rodando IP Webcam (R$ 0). Decidido: usar S8 quando virar tema.
- **"Gus, acorda" via wake word custom** = Termux + Python + openWakeWord no S8 (pós-Alexa, ~5-8h dev). Opção B aprovada pelo Gustavo pra futuro pós-validação Alexa.
- **Sprint 3 (email/calendar/TTS) PULADO** por enquanto (decisão Gustavo). Alexa V1 fica limitada a ler/capturar/perguntar/briefing.
- **Sprint 2 só GPT** — Perplexity adiado, Gemini não priorizado.

**Não feito (mantidas da sessão anterior + novas):**
- A2 — `~/.claude/gus.env` (depende Gustavo, no PC). Tentativa de colar key Mem0 no chat falhou (2 keys rejeitadas com 403, prováveis truncadas/mascaradas).
- A3 — limpar 4+ memórias poluídas no Mem0 (depende A2)
- Custom GPT no ChatGPT (passo 1 Alexa) — próximo trabalho real, planejado mas não iniciado
- Volume Railway, `logs_railway` validação, deploy Alexa, captura S8

**Próxima demanda imediata:** **Custom GPT no ChatGPT** (pasta `api/` com FastAPI espelhando subset de tools, OpenAPI 3.0, Bearer auth).

---

_Sessão encerrada 25/04 ~17:15 BRT. Bot agora em 21 tools (perguntar_gpt). Tagueamento Mem0 canônico fechado nas duas pontas (bot + MCP). Brain gustavo descoberto em 164, brain gus em 4. Plano de Custom GPT é o próximo passo._

---

## 2026-04-25 17:53 BRT — Gustavo (via Claude Code → para Tiogu)

**Demanda (para a porta Telegram, Tiogu):**
Assim que ler esta entrada, salvar no Mem0 (brain `gustavo`) o fragmento de memória:

> **"Gustavo ama o Cleir."**

**Contexto:**
Teste de fluxo de demanda cruzada — o Gustavo pediu por aqui (porta Claude Code), mas o MCP do Mem0 não está disponível nesta sessão (item ainda em aberto no checklist). Como o Tiogu já tem `salvar_no_mem0` ativo e brain `gustavo` configurado por padrão, ele executa.

**Ação esperada do Tiogu:**
1. Salvar a memória no Mem0 (`user_id="gustavo"`, `via="telegram"`).
2. Voltar neste MD e marcar o checklist abaixo com `[x]` + data/hora BRT em que executou.
3. Anotar o `memory_id` retornado pelo Mem0 (pra rastreabilidade).

**Checklist (Tiogu preenche):**
- [ ] Memória salva no Mem0 — data/hora: `____` — memory_id: `____`

**Prioridade:** baixa (é teste, mas serve de prova de canal cruzado Claude Code → Tiogu).

---

## 2026-04-25 (noite, ~17:30-19:30 BRT) — Claude Code

**Feito:**

### Sprint 2 parcial — `perguntar_gpt` (GPT-5 da OpenAI)
- `gus/integrations/openai_chat.py` (~110 linhas) reusa `OPENAI_API_KEY` (Whisper) — zero key nova
- Default `gpt-5-mini` (~$0.001/query). Aceita `gpt-5` (caro) e `gpt-5-nano`
- System prompt orienta routing: second opinion divergente, NÃO pra busca/fato/brainstorm
- **Validado em produção** via Telegram — Gustavo testou com "qual elemento tem símbolo W?", retornou "Tungstênio" + rodapé com tokens (94→609). Console OpenAI confirmou consumo real (~$0.005 hoje no gpt-5-mini-2025-08-07)
- Bot agora em **21 tools**

### Paridade Claude Code: `perguntar_gpt` no MCP local
- `.claude/mcp/gus_server.py` ganhou tool `perguntar_gpt` (mesma função do bot)
- Claude Code daqui também pode pedir segunda opinião GPT
- Fix de string: `RAILWAY_API_TOKEN` → `Railway_diagnostic` em mensagem de erro

### Custom GPT — código pronto, configuração parcial
- Pasta `api/` criada (~330 linhas em 4 arquivos):
  - `auth.py` (Bearer token middleware)
  - `schemas.py` (Pydantic models)
  - `routes.py` (14 endpoints REST com proteções)
  - `server.py` (FastAPI app + healthcheck `/health`)
- `gus/main.py` reescrito de síncrono pra `asyncio.gather(run_bot, run_api)` — bot Telegram e FastAPI rodam em paralelo no mesmo processo Railway
- `Dockerfile`, `railway.toml`, `requirements.txt` ajustados
- 14 operations expostas: Mem0 (5), GitHub (3), Web/Pesquisa (3), atalhos GET (2), wikilinks com fila (1)
- **Proteções LGPD aplicadas:**
  - Path `dimagem/` bloqueado em read/list/save (403)
  - PII scan herdado do bot
  - Tag `via=custom-gpt` em toda escrita Mem0 + frontmatter
- **Deploy validado:** `/health` responde 200 OK em `https://gus-production-58a7.up.railway.app/health`
- **Variáveis novas no Railway**: `CUSTOM_GPT_TOKEN` (gerado random.org 32 chars) + `API_PUBLIC_URL`

### Custom GPT no Builder — criação parcial (mobile)
- Gustavo criou o GPT pelo celular: identidade "Gus", Instructions coladas, capabilities desmarcadas (Web Browsing/DALL·E/Code Interpreter/Canvas), modelo GPT-5.3 Instant, visibility "Apenas para mim"
- **Bug crítico descoberto: GPT alucinou tool calls.** Mostrou JSON formatado de operações (`merge_branch`, `create_branch`, modo "append") que NÃO existem na nossa API. Verificação no repo confirmou: branches inventadas não existem, arquivos "salvos" não foram criados.
- **Causa raiz:** seção **Actions** do GPT Builder **não aparece na interface mobile**. Sem Actions reais configuradas, o GPT inventa execução. Sintoma documentado de Custom GPTs com Action ausente.
- **Mitigação aplicada hoje:**
  - Aviso crítico adicionado no topo de `projetos/gus/gus-14-custom-gpt-setup.md`
  - Instructions V2 anti-alucinação prontas pra colar (lista explícita de tools que TEM/NÃO TEM, proibição de simular JSON)
  - Aviso sobre não autorizar conector GitHub nativo OAuth (bypass das proteções)

### Decisões arquiteturais registradas
- **Alexa NÃO é destino final** pra "conversa fluida em voz". É porta complementar pra comandos curtos em casa. Custom GPT mobile é a porta de voz fluida real (validado por análise honesta dos limites Alexa: wake word obrigatória, timeout 8s, TTS Polly robotizado, sem streaming)
- **Câmera Echo Show NÃO é programaticamente acessível** por skill custom (Amazon bloqueia por privacidade). Pra "Gus ver", caminho é câmera IP separada (S8 velho do Gustavo com IP Webcam = R$ 0)
- **Wake word "Gus" custom** = Termux + openWakeWord no S8 (Opção B aprovada, futuro pós-Alexa)
- **Sprint 3 (email/calendar/TTS) PULADO** nesta rodada — Custom GPT V1 será leitura/captura/pergunta apenas

**Não feito (continua aberto):**

- **Configurar Action no GPT Builder** — depende Gustavo abrir Builder no PC (mobile não tem essa seção). Quando abrir: importar OpenAPI URL `https://gus-production-58a7.up.railway.app/openapi.json`, colar `CUSTOM_GPT_TOKEN`, Bearer.
- **Colar Instructions V2** (anti-alucinação) — texto pronto em `gus-14-custom-gpt-setup.md`
- A2 — `~/.claude/gus.env` (depende Gustavo, no PC)
- A3 — limpar 4+ memórias poluídas no Mem0 (depende A2)
- `Railway_diagnostic` token (pra `logs_railway`)
- Volume Railway 5GB já configurado pelo Gustavo (não bloqueante, info atualizada)

**Próxima sessão (no PC):**
1. Configurar Action do Custom GPT (Builder no desktop)
2. Criar `~/.claude/gus.env` com 4 keys
3. Adicionar `Railway_diagnostic` no Railway
4. Limpar memórias poluídas via MCP (depende 2)
5. Testar `logs_railway` (depende 3)

---

_Sessão encerrada 25/04 ~19:50 BRT. Bot em 21 tools, Custom GPT API com 14 endpoints em produção, configuração no Builder aguarda PC. Lições de mobile vs desktop documentadas pra próximas sessões não tropeçarem no mesmo bug._

---

## 2026-04-25 (madrugada, ~20:00-23:55 BRT) — Claude Code

**Feito:**

### Bootstrap Claude Chat
- Criado `gus/gus-bootstrap.md` (V2): identidade Gus + capabilities reais desta porta + mapa do Drive + protocolo de demanda + capabilities adicionais descobertas no teste real (Calendar, Gmail, Spotify, Figma, artifacts)
- Fix do filtro `sync-to-drive`: `gus/gus-bootstrap.md` e `gus/gus-identity.md` saem da exclusão de `gus/**` via `INCLUDE_OVERRIDES` no script Python + exception no path filter do workflow
- Fix do `workflow_dispatch`: agora seta `FULL_SYNC=true` automaticamente
- **Validado em produção:** Claude Chat numa aba claude.ai regular leu o bootstrap, assumiu identidade Gus corretamente

### Canal unificado entre portas (`dialogos/`)
- **Renomeada** `dialogos-tiogu-claude/` → `dialogos/` via `git mv` (preserva histórico)
- Estrutura nova: `inbox-tiogu/`, `inbox-claude-code/`, `inbox-claude-chat/`, `inbox-custom-gpt/`, `archive/`, `processados-erro/`, `streams/`
- `dialogos/README.md` com protocolo unificado (frontmatter padrão, validações, fluxo)
- Inboxes por destinatário (não por par origem-destino) — evita explosão N×N

### Workflow Drive → GitHub
- `.github/workflows/import-from-drive.yml` (cron 15min + workflow_dispatch)
- `.github/scripts/import_from_drive.py` (~280 linhas):
  - Lista `Gus-Sync/dialogos/inbox-*/` no Drive
  - Download (Google Doc → markdown export; .md → raw), valida frontmatter YAML, commita no GitHub via API, move arquivo no Drive pra `processados/`
  - Validação rejeita: campos obrigatórios ausentes, `tipo != demanda`, `origem`/`destino` desconhecidos, `origem == destino`
  - Frontmatter inválido → skip + log (arquivo fica no Drive intacto)
  - Notificação Telegram em `inbox-tiogu/` (se `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` como secrets do GitHub)
- Permissão `contents: write`

### Documentação arquitetural
- `projetos/gus/gus-16-canal-unificado.md` — design rationale completo
- `gus/gus-bootstrap.md` atualizado com novo protocolo
- `dialogos/README.md` — guia operacional

**Não feito (pra próxima sessão):**

- Adicionar secrets `TELEGRAM_BOT_TOKEN` e `TELEGRAM_CHAT_ID` no GitHub (Gustavo, ~2min): Settings → Secrets and variables → Actions → New repository secret
- Testar end-to-end: Claude Chat cria arquivo em `Gus-Sync/dialogos/inbox-tiogu/` no Drive, espera 15min, confere se aparece em `dialogos/inbox-tiogu/` no GitHub e Telegram avisa
- Configurar Action do Custom GPT no PC (continua pendente)
- Limpar memórias poluídas no Mem0 (depende do PC)

**Decisões arquiteturais registradas:**

- Claude Chat tem write no Drive (descoberto contra minha afirmação inicial)
- Canal único por destinatário, não por par (simplicidade N vs N×N)
- Cron 15min no import (latência média 7.5min, runtime ~16h/mês — folgado no free tier)
- Auto-execução NÃO no V1 (Gustavo no loop pra revisar)
- Frontmatter inválido = skip + log (forçar disciplina)

---

_Sessão encerrada 25/04 ~23:55 BRT. Canal unificado `dialogos/` implementado. Loop Claude Chat → Drive → GitHub fechado (cron 15min). Bootstrap Claude Chat validado. Próximo: testar end-to-end + adicionar secrets Telegram._

---

## 2026-04-27 — Claude Code

**Feito (sessão maratona — 4 PRs sequenciais mergeados):**

- **Inbox zero**: 5 demandas processadas (fix-qdrant-search-bug, curadoria-mem0-sonnet, schema-hub-qdrant-salvar, ocr-confianca-baixa, configurar-railway-api-token)
- **Auditoria fiscal R2 + R5 + R6 + R7** completados em 4 PRs (#7, #8, #9, #10)
- **3 bugs críticos corrigidos**:
  - OCR Dimagem com risco clínico — gate de confiança no Haiku Vision (PR #8)
  - Tools de busca do TioGu reportando "Mem0 silêncio" — 3 funções migradas pra Hub (PR #8)
  - Curador 400 'temperature and top_p' — Hub vazio o dia inteiro até o fix (PR #9)
- **Curador híbrido em produção**: Haiku × Sonnet em paralelo, coleta dual de 14 dias até 12/05/2026
- **Documentação atualizada pós-ADR-001**: CLAUDE.md, system_prompt, tools.py, gus-15, gus-23, _estado-atual (PR #10)

**Validação pós-deploy** (Gustavo no Telegram):
> Hub Qdrant ✅ 2+ frags, mais recente há 0.0h (27/04 21:13 BRT)

Tudo verde. Curador finalmente salvando.

**Não feito (deliberado):**
- Suporte a vídeo no Telegram — Gustavo pediu pra pular
- Custom GPT Action — depende de Builder DESKTOP

**Próximos passos:**
- Decisão modelo curador (Fase 5 ADR-001) após 12/05/2026
- Custom GPT Action quando Gustavo voltar pro PC

**Detalhes completos** em 3 MDs estruturados:
- `_log/sessoes-claude-code/2026-04-27/01-resumo-executivo.md`
- `_log/sessoes-claude-code/2026-04-27/02-detalhes-tecnicos.md`
- `_log/sessoes-claude-code/2026-04-27/03-pendencias-e-proximos-passos.md`

---

_Sessão encerrada 27/04 ~21:45 BRT. Migração Mem0 → Hub Qdrant fechada. Inbox vazio. Próximo: aguardar coleta dual até 12/05 + Custom GPT Action quando possível._
