---
data: 2026-04-25
fonte: bot Telegram (Haiku 4.5)
tipo: log-resumos-mem0
---

# Resumos pro Mem0 — 25/04/2026

## 13:37:52 BRT — salvo
**Turnos:** 6
**Resumo:**
> 1. Conta Anthropic está sem créditos — Gustavo precisa ativar auto-reload em console.anthropic.com/settings/billing para evitar interrupções futuras.
> 2. Gustavo usa wikilinks em projetos/gus/_estado-atual.md e prefere conectar registros de sessão (ex: [[2026-04-24]]).
> 3. Workflows disponíveis no sistema: meta-memoria.yml, briefing-matinal.yml, retrospectiva-semanal.yml, reflexao-quinzenal.yml, export-mem0.yml, sync-to-drive.yml (não existe check-saude.yml).

## 13:41:05 BRT — salvo
**Turnos:** 6
**Resumo:**
> 1. O workflow correto para auditoria é `auditoria-mem0.yml`, não `meta-memoria.yml`
> 2. O workflow `check-saude.yml` existe (Gus havia cometido erro ao dizer que não existia)
> 3. Gustavo autorizou a execução do `auditoria-mem0.yml` e o workflow foi disparado com sucesso

## 13:43:04 BRT — salvo
**Turnos:** 6
**Resumo:**
> 1. Gustavo quer monitorar workflows em tempo real — Gus não recebe notificações automáticas, mas pode verificar com `list_commits` quando solicitado, ou o workflow pode enviar mensagem no Telegram se configurado.
> 
> 2. Commit `e6f86d8` (13:35 de hoje) ativou header `anthropic-beta: token-efficient-tools` no bot — reduz 14-34% do custo de input tokens em tool calls; já está em produção no bot Telegram, mas não nesta sessão de Claude Chat.
> 
> 3. Bot tem `MAX_HISTORY = 40 mensagens` (20 turnos) em RAM — histórico zera em redeploy.
> 
> 4. Há pendência de integração: `bot.py` não integrou `dimagem.py` ainda — `handle_photo` vai direto pra `processar_imagem` sem detecção de OS.
> 
> 5. Gustavo quer verificar branches por arquivos novos/diferentes para analisar mudanças recentes.

## 14:15:55 BRT — salvo
**Turnos:** 6
**Resumo:**
> 1. Gustavo tem um roadmap consolidado pra levar o Gus até Alexa em 4 passos: Custom GPT → Volume Railway → Executor de ação → Alexa Skill + Lambda
> 2. Meta cronológica: Custom GPT + Volume até 28/abr, Executor até 5/mai, Alexa até 12/mai
> 3. Bloqueios conhecidos: Google Cloud policy restringe Drive/Calendar/Gmail OAuth; token Railway de teste foi revogado; Volume Railway ainda não implementado
> 4. Preferência de segurança: quer escopos mínimos no OAuth (ex: Gmail só com permissão "send", sem leitura) e ações de alto risco com pausa pra confirmação
> 5. Preocupação validada: entende que LLM vê conteúdo das mensagens mas não acessa tokens OAuth; confia no modelo de execução descentralizada (servidor Railway executa, Anthropic só processa texto)
> 6. Próximo passo acordado: começar pelo Custom GPT (Passo 1) ainda nesta sessão

## 14:26:17 BRT — salvo
**Turnos:** 6
**Resumo:**
> 1. Gustavo tem preocupação ativa com privacidade de dados — quer entender exatamente o que APIs externas (Anthropic) acessam de seus emails e dados pessoais.
> 
> 2. Decisão tomada: por enquanto implementar APENAS `gmail.send` no bot. Leitura de Gmail fica para depois, com filtragem local de conteúdo sensível antes de mandar pro Claude (mitigação de privacidade).
> 
> 3. Arquitetura atual do bot: 18 tools ativas (eram 13), com 5 novas adicionadas hoje — `list_branches`, `auto_diagnostico`, `logs_railway`, `sugerir_wikilinks`, `deletar_memoria`.
> 
> 4. Nova infraestrutura Railway: bot persiste state em `/app/data/bot_state.json` com write atômico, sobrevive redeploys.
> 
> 5. Scanning automático de dados sensíveis implementado — `save_to_github` bloqueia se detecta CPF, CNPJ, cartões, API keys antes de salvar.
> 
> 6. Token-efficient-tools (header beta Anthropic) ativo — redução de ~14-34% no custo de tool calls.
> 
> 7. Workflows passaram de 6 para 8: `check-saude.yml` e `sync-to-drive-full.yml` são novos, ambos em produção.
> 
> 8. Log auditável de resumos Mem0 em `_log/resumos-mem0/AAAA-MM-DD.md` (integrado com Obsidian).
> 
> 9. `_estado-atual.md` desatualizado (reflete sessão 23-24/abr, não 25/abr) — precisa atualização.
> 
> 10. Próxima prioridade confirmada: Custom GPT no ChatGPT (Passo 1 do caminho Alexa), ~3-4h de código + 20min de ação.

## 14:39:13 BRT — salvo
**Turnos:** 6
**Resumo:**
> 1. Gustavo reconhece padrão pessoal de abrir muitas frentes rápido sem consolidar as anteriores — prefere ser alertado sobre isso e está disposto a mudar o ritmo (fechar loops antes de expandir).
> 
> 2. Próximas prioridades técnicas: integrar `dimagem.py` no `bot.py`, testar `check-saude.yml`, atualizar `_estado-atual.md` antes de partir pro Custom GPT.
> 
> 3. Gustavo tem curiosidade técnica em múltiplos modelos de IA — quer explorar segundo agente (DeepSeek ou GPT-4o) com mesmo acesso a tools/GitHub/Mem0 pra "segundo olhar" no código.
> 
> 4. Gustavo priorizaria alternativa mais segura: GPT-4o em vez de DeepSeek — por questões de privacidade (dados sensíveis do Dimagem não devem passar por servidores chineses).
> 
> 5. Próximo refactor técnico planejado: estruturar `llm.py` pra suportar múltiplos providers LLM com schemas de tools compatíveis (OpenAI/DeepSeek vs Anthropic).

## 14:43:52 BRT — salvo
**Turnos:** 6
**Resumo:**
> 1. Gustavo mora em Rua Barão da Torre, 185, Ipanema, Rio de Janeiro
> 2. Gustavo prefere deixar refatoração de `llm.py` (abstração multi-provider) na fila de próximos passos — não é urgente
> 3. Gustavo está aberto a usar GPT-4o além de Claude; aceitaria DeepSeek ou outros modelos com schema OpenAI padrão
> 4. Gustavo pediu cinemas e filmes em cartaz perto de casa — indicou interesse em recomendações de lazer locais
